#include <stdio.h>
int  main()
{
int a, b, c, max;
printf("Введіть три числа\n");
scanf("%d%d%d", &a, &b, &c);
if (a > b) {
   if (c > a){
       max = c;
   }
   else{
       max = a;
   }
}
else {
    max = b;
}
  printf("Найбільше число %d", max);
}
